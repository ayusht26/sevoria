import HeroText from "@/components/ui/hero-shutter-text";

export default function DemoOne() {
  return (
    <main className="h-screen w-screen overflow-hidden bg-canvas flex items-center justify-center">
      <HeroText text="Seviora" textSizeClass="text-6xl font-bold" />
    </main>
  );
}
